let $map = document.querySelector("#map");
class GoogleMap {
    /**
     * Charge la carte sur un élément
     * @param element
     */
    load (element){

    }
}

if (map != null){
    let map = new GoogleMap();
    map.load($map);
}